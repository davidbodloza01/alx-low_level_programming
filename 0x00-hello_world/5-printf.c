#include <stdio.h>

/**
 * main - an entry point to an application
 *
 * Return: 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
